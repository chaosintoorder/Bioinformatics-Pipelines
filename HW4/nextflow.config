params {
    reads = ''
    accession = ''
    samplesheet = ''
    reference = ''
    outdir = 'results'
    threads = 4
    save_mpileup = false
}

process {
    executor = 'local'
    cpus = params.threads
    memory = '8 GB'
    time = '4h'
}